server:
  port: 8191
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NandabasappaHalli/config-repo
          clone-on-start: true
          search-paths:
            - "*service"
#          default-label: main
