spring:
  application:
    name: orderservice

    # datasource:
    #  hikari:
    #   data-source-properties:
  #       gssEncMode: disbale
  #       gsslib: disable
  cloud:
    config:
      uri: http://localhost:8191
      name: orderservice
      profile: dev

  profiles:
    active: dev
